package week2;

public class DoubleDemo {
    public static void main(String[] args) {
        // try change the below value to 0.2
        double total = 0.25;
        for (int i = 0; i < 100; i++) {
            // try change the below value to 0.2
            total += 0.25;
            System.out.println("total = " + total);
        }
    }
}
